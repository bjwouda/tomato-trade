{{!-- The game is only used in one language at the moment. Some of the other language translations may be missing. --}}
{{!--
<div class="container-fluid">
  <div class="row text-right">
    <a href="#" class="{{if (eq i18n.locale 'en') 'lead'}}" {{action "changeLang" 'en'}}>EN</a>
    <a href="#" class="{{if (eq i18n.locale 'nl') 'lead'}}" {{action "changeLang" 'nl'}}>NL</a>
  </div>
</div>
--}}
{{outlet}}
