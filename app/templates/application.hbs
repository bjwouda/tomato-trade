{{outlet}}

<div class="container-fluid">
  <div class="row">
    <div class="col-12 col-sm-12 col-lg-12">
      <div class="text-center">
        <a href="#" {{action "changeLang" 'en'}}>{{world-flag "gb" 32}}</a>
        <a href="#" {{action "changeLang" 'nl'}}>{{world-flag "nl" 32}}</a>
      </div>
    </div>
  </div>
  <div class="row">
    <div class="col-12 col-sm-12 col-lg-12">
      <div class="text-center">
        {{version}}
      </div>
    </div>
  </div>
</div>
