import storeWithWeek from 'tomato/utils/store-with-week';
import { module, test } from 'qunit';

module('Unit | Utility | store with week');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = storeWithWeek();
  assert.ok(result);
});
