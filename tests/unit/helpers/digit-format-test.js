import { digitFormat } from 'tomato/helpers/digit-format';
import { module, test } from 'qunit';

module('Unit | Helper | digit format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = digitFormat([42]);
  assert.ok(result);
});
