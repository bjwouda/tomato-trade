import { numModulo } from 'tomato/helpers/num-modulo';
import { module, test } from 'qunit';

module('Unit | Helper | num modulo');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = numModulo([42]);
  assert.ok(result);
});
