
import { tutorialState } from 'tomato/helpers/tutorial-state';
import { module, test } from 'qunit';

module('Unit | Helper | tutorial state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tutorialState([42]);
  assert.ok(result);
});

