
import { notNull } from 'tomato/helpers/not-null';
import { module, test } from 'qunit';

module('Unit | Helper | not null');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notNull([42]);
  assert.ok(result);
});

