
import { notUndefined } from 'tomato/helpers/not-undefined';
import { module, test } from 'qunit';

module('Unit | Helper | not undefined');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = notUndefined([42]);
  assert.ok(result);
});

