
import { tutorialFirstState } from 'tomato/helpers/tutorial-first-state';
import { module, test } from 'qunit';

module('Unit | Helper | tutorial first state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tutorialFirstState([42]);
  assert.ok(result);
});

