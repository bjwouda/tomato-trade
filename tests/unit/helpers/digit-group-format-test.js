import { digitGroupFormat } from 'tomato/helpers/digit-group-format';
import { module, test } from 'qunit';

module('Unit | Helper | digit group format');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = digitGroupFormat([42]);
  assert.ok(result);
});
