import { myLte } from 'tomato/helpers/my-lte';
import { module, test } from 'qunit';

module('Unit | Helper | my lte');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = myLte([42]);
  assert.ok(result);
});
