
import { tutorialLastState } from 'tomato/helpers/tutorial-last-state';
import { module, test } from 'qunit';

module('Unit | Helper | tutorial last state');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = tutorialLastState([42]);
  assert.ok(result);
});

